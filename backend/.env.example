# Copy this file to .env and fill in real values
# Used by backend scripts and your app (if you use dotenv)
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=cs490
PGPORT=5432

# Optional: set a DATABASE_URL if your app expects it
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/cs490
